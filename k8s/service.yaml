apiVersion: v1
kind: Service
metadata:
  name: workshop-api
  labels:
    app: workshop-api
spec:
  type: ClusterIP
  selector:
    app: workshop-api
  ports:
    - name: http
      port: 80
      targetPort: http
